# Frontend Environment Configuration for Single Deployment
# When frontend and backend are served from same URL/domain

# Use relative path /api since both frontend and backend are on same domain
VITE_API_BASE_URL=/api
